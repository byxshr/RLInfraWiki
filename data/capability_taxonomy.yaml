capabilities:
  rollout-batched-sync: [rollout, inference]
  rollout-server-async: [rollout, scheduler]
  rollout-agentic-multiturn: [rollout, environment]
  rollout-logprob-capture: [rollout, data-buffer]
  weight-sync-disk: [weight-sync, checkpoint]
  weight-sync-tensor: [weight-sync, inference]
  weight-sync-distributed: [weight-sync, rollout]
  weight-sync-delta: [weight-sync, checkpoint]
  policy-versioning: [training, rollout]
  pause-update-resume: [weight-sync, rollout]
  reward-verifier: [reward, verifier]
  data-buffer-trajectory: [data-buffer, rollout]
  sample-grouping: [data-buffer, algorithm]
  checkpoint-conversion: [checkpoint, training]
