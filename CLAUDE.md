# RLInfraWiki

Use `scripts/compose_context.py` for design/adaptation tasks and `scripts/validate_context_bundle.py` before answering. Known-target tasks must include Target Framework, Generic Infra, Cross-Framework, and Validation & Risk packs.

Set `RLINFRA_WIKI_ROOT=/absolute/path/to/RLInfraWiki` only when running scripts from outside the skill checkout. Invalid root settings fail hard.
