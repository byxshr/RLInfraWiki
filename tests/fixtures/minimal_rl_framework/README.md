# minimal rl framework

This fixture is a tiny RL framework shape for smoke tests. It has trainer, rollout, reward, and data-buffer vocabulary plus an intentional gap around SGLang weight synchronization.
