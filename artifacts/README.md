# Artifacts

Small excerpts, configs, or generated snippets may live here when they have a `PROVENANCE.yaml` and, for multi-file bundles, a `MANIFEST.yaml`. Do not vendor full upstream framework repositories.
